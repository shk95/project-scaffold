{
  "$comment": [
    "Merge into permissions.allow, alongside the stack-agnostic entries from",
    "core. Everything below inspects, builds or formats — nothing here",
    "mutates the running machine.",
    "",
    "Deliberately absent: `nix flake update` (rewrites flake.lock to newer",
    "input revisions — a dependency-version decision, the same category as",
    "`flutter pub upgrade`, which the Flutter overlay likewise leaves out) and",
    "`home-manager switch` / `darwin-rebuild switch` / `nixos-rebuild switch` /",
    "`nix-collect-garbage` (these mutate the running machine or delete store",
    "paths, the same reasoning as core leaving out `git push`)."
  ],
  "permissions": {
    "allow": [
      "Bash(nix flake check:*)",
      "Bash(nix flake show:*)",
      "Bash(nix flake metadata:*)",
      "Bash(nix build:*)",
      "Bash(nix eval:*)",
      "Bash(nix run:*)",
      "Bash(nix fmt:*)",
      "Bash(nix develop:*)",
      "Bash(nix-instantiate --parse:*)",
      "Bash(direnv allow:*)"
    ]
  }
}
